---
phase: 03-authentication
plan: 12
type: execute
wave: 7
depends_on: ["03-03", "03-10", "03-11"]
files_modified: [addon.xml, README.md, VENDORED.md]
autonomous: true
requirements: [AUTH-02, AUTH-23, VND-09, DIST-01]
security_asvs_level: 1
security_block_on: high

must_haves:
  truths:
    - "The manifest's disclaimer no longer tells users their tokens travel through a third-party sign-in server, because they no longer do. Leaving it would be a statement about where credentials go that is false in the direction that matters (AUTH-23)"
    - "The new disclaimer describes what actually happens: a device authorization grant straight to the identity provider, a public application identifier, no server in the middle and no credential of any kind in the add-on"
    - "The project's own description of how signing in works matches the flow that ships"
    - "Every local modification this phase made to a vendored file is recorded, including the deletion of two whole files, because the modification record is the only thing that makes the vendored tree auditable against upstream (VND-09)"
    - "The two documents that describe the old flow are in the excluded set and are therefore the only place the old vocabulary may still appear; they describe it as history, and the positive assertion from plan 03-03 requires them to describe the new flow as well (D-09, Finding F)"
    - "The build and install procedure is written down, because the archive exists as of plan 03-02 and the person installing on the television needs to know the command (DIST-01)"
    - "The manifest's identity, imports and licence rows are untouched; the gates asserting them stay green"
  artifacts:
    - path: "addon.xml"
      provides: "The manifest, with a disclaimer describing the flow that ships and release notes naming the change"
      contains: "<disclaimer"
    - path: "VENDORED.md"
      provides: "The modification record extended with this phase's changes to the vendored tree, including two deletions"
      contains: "signin"
  key_links:
    - from: "README.md"
      to: "docs/AZURE-REGISTRATION.md"
      via: "the readme links the registration runbook, which plan 03-03 created"
      pattern: "AZURE-REGISTRATION"
    - from: "VENDORED.md"
      to: "resources/lib/vendor/clouddrive_common/"
      via: "every local modification to a vendored file has a row"
      pattern: "clouddrive_common"
  prohibitions:
    - requirement_id: AUTH-23
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT leave a disclaimer describing a data flow that no longer happens. A user reading it is being told their credentials pass through a party they can no longer verify, and the direction of the error does not make it acceptable"
    - requirement_id: VND-09
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT erase the modification record's description of the old flow. That document is the attribution and modification record; deleting a file is itself a modification and the record is where it is stated"
---

<objective>
Make the documents tell the truth about a system that changed underneath them.

Purpose: the manifest currently carries a disclaimer telling users their tokens go to a third-party
sign-in server, with a link to that server's source and an invitation to host their own. That was
accurate. As of this phase it is not, and it is inaccurate in the direction that matters — it makes a
claim about where a user's credentials travel. The readme describes the same flow. And the vendored
tree's modification record has to gain this phase's rows, including two whole-file deletions, because
that record is the only thing that lets the vendored copy be diffed against upstream and understood.

This plan runs last among the code-adjacent ones because it records what the others did.

Output: three documents that match the shipped system.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-authentication/03-RESEARCH.md
@.planning/phases/03-authentication/03-08-SUMMARY.md
@.planning/phases/03-authentication/03-09-SUMMARY.md
@.planning/phases/03-authentication/03-11-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The manifest's disclaimer and release notes</name>
  <read_first>
    - `addon.xml` in full — fifty-nine lines; the identity row, the two imports, the two localised descriptions, the licence and source rows, the release notes and the disclaimer
    - `tests/test_vendor_gates.py::test_addon_xml_identity` and `::test_addon_xml_imports` — exactly which rows are asserted, so this edit stays clear of them
    - `.planning/research/PITFALLS.md`, the security-mistakes section — the entry naming a disclaimer that describes a data flow that no longer happens
  </read_first>
  <action>
    Rewrite the disclaimer. Describe what the add-on now does: it signs in with a device authorization
    grant directly to the identity provider, using an application identifier that is public by design;
    no server operated by this project or anyone else sits in the exchange; no password is ever seen by
    the add-on; and the permission requested is read-only access to the user's own files. Say plainly
    that this project is not affiliated with the provider. Drop the link to the third party's source
    and the invitation to host a copy of it, which now describe nothing.

    Keep it short. It is rendered in an add-on browser on a television, not in a browser.

    Extend the release notes with a line naming the change in user-facing terms — sign-in now goes
    directly to the provider and no longer passes through a third-party server — because that is the
    one change in this phase a user could otherwise only discover by reading source.

    Leave the identity, version, provider, imports, licence, source and forum rows exactly as they are.
    Two gates assert them and this edit has no business near either.
  </action>
  <verify>
    <automated>python -m pytest tests/test_vendor_gates.py -q -k addon_xml</automated>
    <automated>python -c "import xml.etree.ElementTree as ET; ET.parse('addon.xml'); print('parses')"</automated>
    <automated>python -m pytest tests/test_auth_gates.py -q -k broker</automated>
  </verify>
  <done>The disclaimer describes the flow that ships, the release notes name the change, the manifest parses, and both identity gates plus the broker sweep are green.</done>
</task>

<task type="auto">
  <name>Task 2: The readme — the flow, the runbook link, and how to build and install</name>
  <read_first>
    - `README.md` in full, particularly the paragraph describing sign-in
    - `tools/build_addon_zip.py` as plan 03-02 left it — the command and its output name
    - `docs/AZURE-REGISTRATION.md` as plan 03-03 left it
    - `tests/gatelib.py` — the readme is in the excluded set, so it may name the old flow; the positive assertion requires it also to name the new one
  </read_first>
  <action>
    Replace the sign-in description with what happens now: a code appears on the television, the user
    opens the address shown on their phone and enters the code, and the add-on holds a refresh token
    locally that it renews by itself. Say that no server other than the provider is involved and that
    the application identifier shipped with the add-on is public on purpose, linking the runbook for
    the reasoning.

    Add a short section on building and installing: the command that produces the archive, the archive's
    name, and that it installs from Kodi's own file manager. The person doing that is standing in front
    of a television with a remote, so keep it to the two facts they need.

    Where the readme refers to the old flow historically, mark it as history rather than deleting it.
    This document is deliberately excluded from the sweep that forbids the old vocabulary, and it earns
    that exclusion by being read by name by a positive assertion; a document that erases what it
    replaced buys the exclusion without paying for it.
  </action>
  <verify>
    <automated>python -m pytest tests/test_vendor_gates.py -q</automated>
    <automated>grep -c "AZURE-REGISTRATION" README.md</automated>
    <automated>python -m pytest tests/test_auth_gates.py -q -k broker</automated>
  </verify>
  <done>The readme describes the shipped flow, links the runbook, documents the build and install, and the broker sweep is green with the readme still excluded and still positively asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Record every modification this phase made to the vendored tree</name>
  <read_first>
    - `VENDORED.md` in full — the section structure, the pinned upstream revision, the modification rows and their format, and the two places describing the old flow
    - `tests/test_vendor_gates.py::test_vendored_sha_recorded` and `::test_vendored_md_sections` — what the record is required to contain
    - The summaries of plans 03-06, 03-08 and 03-09 — the authoritative list of what changed in the vendored tree and why
  </read_first>
  <action>
    Add a row for every vendored file this phase touched: the dialog class, gaining setters and losing a
    focus call from its per-tick update, with its destructor guarded; the provider, with three methods
    rewritten and renamed and two token accessors repointed; the transport, gaining a short retry
    profile argument and a redactor covering five field names; the plugin's user-interface module, with
    the sign-in flow rewritten, the account list extended, the router's dispatch replaced and the
    failure handler rebuilt; and the Kodi utilities module, losing one accessor.

    Add rows for the two deletions — the module that only ever posted to the third party, and the error
    reporter whose only address source went with it. A deletion is a modification and the record is
    where it is stated; an unrecorded deletion is the one difference from upstream that a diff cannot
    explain, because the file is simply absent.

    Update the two places describing the old flow to describe it in the past tense and name what
    replaced it, including the note explaining why a fresh profile looks empty. Do not erase them.

    Do not recompute or re-pin the recorded upstream digest. Re-pinning after an edit is exactly the
    failure the digest exists to catch, and this phase modified vendored files rather than the pinned
    upstream text.
  </action>
  <verify>
    <automated>python -m pytest tests/test_vendor_gates.py -q -k "vendored"</automated>
    <automated>python -m pytest tests -q</automated>
    <automated>git diff --stat HEAD~1 -- VENDORED.md</automated>
  </verify>
  <done>Every vendored file this phase changed has a row, both deletions are recorded, the old flow is described as history, the pinned digest is unchanged, and the whole suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped documents → user's understanding | The disclaimer is the only statement a user reads about where their credentials go |
| vendored copy → upstream | The modification record is the only thing that makes the difference auditable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-55 | Repudiation | manifest disclaimer | high | mitigate | Rewritten to describe the flow that ships; a disclaimer claiming credentials pass through a third party they no longer reach is a false statement about a data flow |
| T-03-56 | Repudiation | modification record | medium | mitigate | Every changed vendored file and both deletions get a row, so the vendored copy remains diffable against upstream with every difference explained |
| T-03-57 | Information Disclosure | excluded documents | medium | mitigate | The two documents keep their exclusion and keep paying for it by being read by name by a positive assertion that requires them to describe the current flow |
</threat_model>

<verification>
- `python -m pytest tests -q` — the whole suite green, including both manifest gates, all three record assertions and the broker sweep
- `addon.xml` parses and its disclaimer names no third-party sign-in server
- The readme links the runbook and documents the build
</verification>

<success_criteria>
Nothing shipped tells a user something untrue about where their credentials go, and the vendored copy
can still be explained line by line against upstream.
</success_criteria>

<output>
Create `.planning/phases/03-authentication/03-12-SUMMARY.md` when done
</output>
