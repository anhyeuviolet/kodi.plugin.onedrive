---
phase: 01-vendor-lift
plan: 07
type: execute
wave: 4
depends_on: ["01-01", "01-05", "01-06"]
files_modified: [resources/lib/addon.py, VENDORED.md, CREDITS.md, README.md]
autonomous: false
requirements: [VND-07, VND-09, VND-10, ID-04, KODI-02, CI-06]
security_asvs_level: 1
security_block_on: high

user_setup:
  - service: test-hardware
    why: "The install matrix and the acceptance pass run on four Kodi versions and on the Android test phone over adb; neither is reachable from this shell"
    dashboard_config:
      - task: "Attempt installation on portable Kodi 19 and confirm it is refused; install on portable Kodi 20, on Kodi 21.3 and on portable Kodi 22"
        location: "Kodi -> Add-ons -> Install from zip file"
      - task: "Run the enumerated acceptance checklist on Kodi 21.3 on Windows and on the Android phone over adb, and archive both kodi.log files"
        location: "Android phone over adb; pull the log from /sdcard/Android/data/org.xbmc.kodi/files/.kodi/temp/kodi.log, which is the same path on the phone build because the package id org.xbmc.kodi is the same"

must_haves:
  truths:
    - "VENDORED.md carries all six required sections and fails the gate on a missing heading rather than only on file absence, so a stub file cannot pass for a record"
    - "VENDORED.md records the upstream URL, the matrix branch, version 1.4.0 and the commit df68e9a589a6faef2b3228f7520e77729bc05d9b verbatim, so a future diff against upstream starts from a fixed point rather than from a moving branch tip"
    - "The module's own service extension point ran only the directory-listing service with its redirector; that subsystem is out of scope by decision, so nothing survives the fold and this add-on's own service gains nothing — recorded as an explicit decision so a later reader can tell considered-and-discarded from overlooked"
    - "The acceptance checklist is the enumerated list — installs, loads, plugin root renders an empty account list, settings screen renders every row with correct labels, all three dialogs open through the smoke action, the QR image appears under the new profile after that profile directory was deleted first and under a second, different filename on a second invocation in the same session, service starts, no traceback and no unknown-add-on-id error in the log — and it names the two recorded deviations rather than claiming identical behaviour, which is not checkable while the broker is dead"
    - "On a clean profile the plugin root renders an empty account list with an add-an-account row and no error dialog; empty is the correct result, not a failure"
    - "The install matrix and the acceptance checklist are recorded as separate result sets in the same session record, so a successful install does not stand in for a successful acceptance pass"
    - "The session record has a row per platform — Windows on Kodi 21.3 and the Android test phone over adb — and an unfilled row is a phase-gate failure rather than an omission"
    - statement: "The acceptance pass runs on Windows first and on the Android phone second, and both log files are archived with the phase record; a pass on one platform is not evidence for the other, because Kodi's Android build — its storage regime, its profile paths, its file mode bits and its loopback binding — is not reproducible from a Windows desktop, and exercising it is the whole reason the Android leg exists"
      verification: backstop
    - statement: "Both log files are captured after a full pass rather than incrementally, the Android one retrieved by a single adb pull of /sdcard/Android/data/org.xbmc.kodi/files/.kodi/temp/kodi.log so it arrives as a file to grep rather than as something read off a screen, and both are attached to the phase record — no traceback in a full pass is the single most valuable artifact this phase produces, because Phases 3 through 7 will all ask whether something was already broken after vendoring"
      verification: backstop
  artifacts:
    - path: "VENDORED.md"
      provides: "Upstream provenance, per-subtree licences, the exclusion list with reasons, the local-modification table keyed by file, the service-extension-point decision, and the recorded behaviour deviations"
      contains: "df68e9a589a6faef2b3228f7520e77729bc05d9b"
    - path: "CREDITS.md"
      provides: "Origin, bundled module, bundled QR encoder and its bundled PNG writer, each with its licence named"
    - path: ".planning/phases/01-vendor-lift/COVERAGE.md"
      provides: "The reasoned declaration that this phase integrates no external API; written at planning time, confirmed still accurate here"
    - path: "README.md"
      provides: "An accurate description of this add-on and the attribution ID-04 requires"
    - path: "resources/lib/addon.py"
      provides: "The phase-local dialog smoke action, without which the third success criterion cannot be exercised at all"
      contains: "_dialog_smoke"
  key_links:
    - from: "VENDORED.md"
      to: "resources/lib/vendor/clouddrive_common/cache/LICENSE"
      via: "the licence table records this file's provenance as unresolved and states that it travels with the cache subtree"
      pattern: "Apache"
    - from: "resources/lib/addon.py"
      to: "resources/lib/vendor/clouddrive_common/ui/dialog.py"
      via: "the smoke action constructs all three dialog classes in turn"
      pattern: "_dialog_smoke"
  prohibitions:
    - requirement_id: VND-10
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT leave the dialog smoke action reachable in a shipped build without it being recorded in VENDORED.md and scheduled for removal — it is a debug affordance that constructs UI with dummy arguments, and an undocumented one becomes permanent by default"
    - requirement_id: VND-09
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT sign off the acceptance pass while a departure from the recorded deviations list is known and unrecorded — the record and the pass are what a later phase reads to decide whether something was already broken after vendoring, and a pass signed over a known gap makes every later attribution unsound. This is the sign-off condition this plan can enforce; the underlying no-undocumented-behaviour-change obligation is owned by plan 01-04 and is not restated here"
    - requirement_id: ID-04
      category: attribution
      status: resolved
      verification: test
      statement: "MUST NOT describe this add-on as original work or omit any of the four upstream attributions — the origin add-on and its author, the bundled module, the bundled QR encoder and the PNG writer bundled inside it — each with its licence named"
    - requirement_id: VND-09
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT assert a provenance the evidence does not support — the Apache-2.0 file beside the cache module is unfilled boilerplate naming nobody while the adjacent module carries a GPL-3.0 header by the module's own author, and that ambiguity is recorded as unresolved rather than resolved by guess"
---

<objective>
Write down what was done and prove it runs. The record is what a future upstream diff, a future
licence question and a future "was that already broken after vendoring?" all start from; the
acceptance pass is the standard every later phase inherits.

Purpose: two of this phase's requirements are documents, and both are documents that a later
reader depends on to distinguish a deliberate decision from an oversight. The third piece is a
test affordance: the QR dialog named in the phase's third success criterion is constructed deep
inside a sign-in flow that runs three lines after a call to a dead broker, so no amount of
clicking reaches it. Asserting the XML files exist proves the copy but not the path argument,
which is the thing that actually breaks — so the dialogs have to be constructed for real.

Output: `VENDORED.md`, `CREDITS.md`, a rewritten `README.md`, a phase-local dialog
smoke action, and one archived acceptance session across four Kodi versions and two platforms.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vendor-lift/01-RESEARCH.md
@.planning/phases/01-vendor-lift/01-VALIDATION.md
@.planning/phases/01-vendor-lift/01-01-SUMMARY.md
@.planning/phases/01-vendor-lift/01-04-SUMMARY.md
@.planning/phases/01-vendor-lift/01-05-SUMMARY.md
@.planning/phases/01-vendor-lift/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the phase-local dialog smoke action</name>
  <read_first>
    - `resources/lib/addon.py` in full — it is 60 lines
    - `resources/lib/vendor/clouddrive_common/ui/addon.py` — the route method and how it maps an action parameter to a method, plus the action-renaming helper this repository overrides
    - `resources/lib/vendor/clouddrive_common/ui/dialog.py` — the three dialog classes, their create methods, and the arguments each constructor takes
    - `.planning/phases/01-vendor-lift/01-RESEARCH.md` Pitfall 6
    - `.planning/research/ANDROID-PLAYBACK-STORAGE.md` sections 6.6 and 7.6 — why the QR dialog is constructed twice here and what the two logged image paths decide
    - `.planning/phases/01-vendor-lift/01-05-SUMMARY.md` — the recorded filename shape the QR image guard produces
  </read_first>
  <files>resources/lib/addon.py</files>
  <action>
    Add a `_dialog_smoke` method to the `OneDriveAddon` class in `resources/lib/addon.py`, reachable
    as the plugin action `_dialog_smoke` on a plugin URL under this add-on's id. Read the base
    class's route method first to learn how an action parameter reaches a method: if it dispatches
    by attribute lookup on the action name, adding the method is sufficient; if it consults an
    action map, register the name there too. Do not guess which.

    The method constructs and shows each of the three dialog classes in turn with dummy arguments
    — the QR sign-in progress dialog, the export schedule dialog and the export main dialog —
    then closes each. Its purpose is to prove three things that nothing else in this phase can
    prove: that each dialog's skin XML resolves under this add-on's own resources directory rather
    than under the uninstalled module's, that the textures each XML references load, and that the
    QR image is written into this add-on's profile directory rather than someone else's. Log the
    resolved skin path and the resolved profile path for each dialog so the acceptance pass has
    something to read rather than only something to look at.

    Wrap each dialog in its own error handler that logs and continues, so one failing dialog does
    not hide the state of the other two.

    The QR dialog is constructed twice in one run of the action, not once, and the resolved image
    path is logged on each construction. Repeat invocation within a single Kodi session is the
    exact shape that surfaces a stale texture — the cache is keyed by path, so a fixed filename
    could render the first image against the second code. Plan 01-05 gives the image a
    per-invocation filename precisely to remove that, and constructing the dialog twice is what
    turns that guard from a claim into an observation: two constructions must log two different
    paths. This is the repository-side half of the check that
    `.planning/research/ANDROID-PLAYBACK-STORAGE.md` section 7.6 runs on the Android device.

    Put the method in `resources/lib/addon.py` rather than in the vendored tree, so it is trivially
    removable and so it does not become another local modification to a vendored file. Mark it in
    the code as phase-local with a comment naming the phase and the reason it exists. Record it in
    VENDORED.md's deviations section in Task 2 with a note that it is scheduled for removal or for
    a debug-only gate — an undocumented debug affordance becomes permanent by default.
  </action>
  <verify>
    <automated>python -c "import ast,pathlib; src=pathlib.Path('resources/lib/addon.py').read_text(encoding='utf-8'); cls=[n for n in ast.parse(src).body if isinstance(n,ast.ClassDef) and n.name=='OneDriveAddon'][0]; names={n.name for n in cls.body if isinstance(n,ast.FunctionDef)}; assert '_dialog_smoke' in names, sorted(names); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast,pathlib; src=pathlib.Path('resources/lib/addon.py').read_text(encoding='utf-8'); cls=[n for n in ast.parse(src).body if isinstance(n,ast.ClassDef) and n.name=='OneDriveAddon'][0]; names={n.name for n in cls.body if isinstance(n,ast.FunctionDef)}; assert '_dialog_smoke' in names; print('ok')"` prints `ok`
    - `grep -c 'QRDialogProgress' resources/lib/addon.py` returns at least 1
    - `grep -c 'ExportScheduleDialog' resources/lib/addon.py` returns at least 1
    - `grep -c 'ExportMainDialog' resources/lib/addon.py` returns at least 1
    - `python -m compileall -q resources/ entrypoint.py service.py` exits 0
    - `python -m pytest tests/test_vendor_gates.py::test_gpl_headers_intact -q` exits 0, confirming the edited file still carries its header
    - `grep -c 'mkdirs' resources/lib/vendor/clouddrive_common/ui/dialog.py` returns at least 1, re-asserting at the phase gate that the QR write creates its profile directory first — plan 01-05 makes this change and this criterion is what stops it being silently lost between wave 3 and the acceptance pass
    - `grep -c 'uuid4' resources/lib/vendor/clouddrive_common/ui/dialog.py` returns at least 1, re-asserting that the image filename is per-invocation rather than one fixed name, which is what makes the twice-constructed QR dialog above a meaningful check
    - The smoke action constructs the QR dialog twice and logs the resolved image path each time, verified by reading the two logged paths in a local run or, failing that, by inspecting the method's structure
    - This task's commit is made as `git commit … && git rev-parse HEAD`, the printed SHA is recorded in the SUMMARY as this task's commit, and `git show --name-only --format= <that SHA>` lists only `resources/lib/addon.py`. The assertion is self-relative rather than `HEAD~1`-relative, matching plans 01-05 and 01-06, so that no criterion in this phase depends on which plan committed last
  </acceptance_criteria>
  <done>A plugin action exists that constructs all three dialogs for real, so the phase's third success criterion is exercisable rather than merely assertable.</done>
</task>

<task type="auto">
  <name>Task 2: Write the vendoring record, the credits and the README</name>
  <read_first>
    - `.planning/phases/01-vendor-lift/01-04-SUMMARY.md`, `01-05-SUMMARY.md` and `01-06-SUMMARY.md` — the local-modification table is assembled from these three records rather than re-derived from the tree
    - `.planning/phases/01-vendor-lift/01-RESEARCH.md` sections "Upstream Tree" including its file inventory table, "Licence Audit", "VND-07 — The Module's Own Service", "Resource Merge Map -> Settings", and "Security Domain"
    - `README.md` in full — it currently advertises upstream features this project has decided to delete
    - `tests/test_vendor_gates.py` — the exact headings `test_vendored_md_sections` looks for and the exact strings `test_credits_content` looks for
  </read_first>
  <files>VENDORED.md, CREDITS.md, README.md</files>
  <action>
    **VENDORED.md.** Six sections, with these exact headings so the gate can find them: `Upstream`,
    `Licences`, `Excluded from the copy`, `Local modifications`, `Service extension point`,
    `Recorded behaviour deviations`.

    Under `Upstream`, record the repository URL `https://github.com/cguZZman/script.module.clouddrive.common`,
    branch `matrix`, version `1.4.0`, commit `df68e9a589a6faef2b3228f7520e77729bc05d9b`, commit
    date 2023-01-21 and commit subject "Kodi 20 fix". Record that upstream has only two branches,
    `krypton` and `matrix`, that no `master` branch exists any longer, and that the raw content URL
    for that dead ref still serves the 1.3.9 Python 2 line — so a reader following VND-01's wording
    about `master` does not conclude the wrong tree was taken. Record the vendored package path
    `resources/lib/vendor/clouddrive_common/` and state that the name is fixed, because reversing
    it means redoing every import.

    Close the same section with a short bolded note — a paragraph, not a new heading, so the six
    headings the gate looks for stay exactly six — reconciling how the repository-wide sweep is
    satisfied. The phase's first success criterion asks for zero repository-wide hits for the
    upstream module id and zero for the dynamic-evaluation construct. Taken literally that is
    unsatisfiable and always was, because this document and `CREDITS.md` are *required* to name the
    upstream module verbatim, and the gate file is required to name the construct it forbids. State
    the operative form plainly: zero hits outside the six record and planning paths the `source_scan`
    helper in `tests/test_vendor_gates.py` excludes — `.planning`, `tests`, `VENDORED.md`,
    `CREDITS.md`, `COVERAGE.md` and `README.md` — and name `test_no_hardcoded_module_id` and
    `test_no_eval` as the operative, runnable form of that criterion. Without this note a later
    verification pass reading the criterion literally fails a phase that is correct.

    Under `Licences`, a table by subtree. This add-on and the vendored module are both
    GPL-3.0-or-later with per-file headers naming the module's author. The upstream `LICENSE.txt`
    is byte-identical to this repository's, so only one copy exists at the root — record that
    identity so a future reader does not conclude a licence file was lost. The Apache-2.0 file
    beside the cache module is preserved verbatim in place: state plainly that its provenance is
    unresolved, that it is unmodified boilerplate whose copyright placeholder was never filled in
    and therefore names nobody, that the adjacent cache module carries a GPL-3.0 header by the
    module's own author, and that this record does not resolve the contradiction. State that the
    file travels with the cache subtree, so if that subtree is dropped in a later phase this file
    goes with it. The vendored QR encoder is BSD-3-Clause, Copyright (c) 2013 Michael Nooner,
    carried in its own `LICENSE.md`; the PNG writer bundled inside it is MIT, Copyright (C) 2006
    Johann C. Rocholl and others, and its in-file header is the notice.

    Under `Excluded from the copy`, list every excluded path with its reason: the upstream
    `resources` package marker, because a second importable top-level package makes imports
    ambiguous; the upstream `settings.xml`, because it declares only the two directory-listing
    settings this add-on already declares and the module reads them unqualified against the
    calling add-on, so its own file was never the live one for this add-on; the `pt_br` language
    file, because additional localizations are out of scope; the upstream `addon.xml`,
    `service.py`, `icon.png` and `LICENSE.txt`; and the upstream repository and IDE metadata.

    Under `Local modifications`, a table keyed by file — not prose. This is the document a future
    upstream diff starts from, and a paragraph cannot be diffed against. One row per modified
    vendored file, naming what changed and why: the anchored import rewrite across the tree; the
    two deleted dead functions in the module's utility file; the eight add-on-id sites and what
    each became; the QR encoder import rewritten to the vendored path; the internal self-imports
    rewritten inside the vendored encoder; the date parse replaced by the standard library, with
    the fractional-seconds truncation and the reason for it; the three dynamic-evaluation reads and
    two matching writes converted to JSON; the named HTTP timeout constant with its value and its
    unmeasured status; and the two QR-image write guards in the dialog module — the profile
    directory created before the write, and the per-invocation filename. Give the two guards their
    own row with their reasoning, because both are edits upstream does not have and a future
    upstream diff will show them: the directory creation exists because a brand-new add-on id means
    a possibly-absent `addon_data` path on first sign-in, and the unique filename exists because
    Kodi's texture cache is keyed by path and a fixed name could render a previous session's image
    against a new code. Record the filename shape as plan 01-05's SUMMARY records it.

    Under `Service extension point`, the VND-07 decision in full. The module's manifest declared a
    Kodi service extension whose entire executable content ran the directory-listing service with
    its redirector variant. That subsystem is out of scope by decision and is deleted outright in
    Phase 7 under KODI-07. Consequently this add-on's own `service.py` gains nothing from the fold
    and the module's `service.py` was not copied. Two sentences are enough, but they must be
    written down — otherwise a later reader cannot distinguish considered-and-discarded from
    overlooked. Note separately that this repository's own `service.py` constructs the same service
    class with a provider argument, which is a different construction that survives this phase
    unchanged because this phase preserves behaviour, and which is deleted in Phase 7.

    Under `Recorded behaviour deviations`, the complete list, because the phase's contract is
    identical behaviour and every departure must be attributable. First: the broker the sign-in
    flow calls has been dead since November 2022, so sign-in, account listing, browsing and
    playback are all unreachable on a fresh profile — inherited, not caused here, and replaced in
    Phase 3. Second: the directory-listing setting now defaults to off rather than on. State the
    reasoning — the service binds loopback on the configured port and serves an enumerable index of
    the whole drive with no authorisation check anywhere in the server base class, on Android every
    installed application can reach loopback, the feature is documented as not working by the
    module's own upstream, and because the add-on id is new no user has a stored value that would
    override the new default — and forward-reference KODI-07 and Phase 7, where the subsystem is
    deleted outright. Third: the settings row that opened the separate common-settings add-on is
    removed along with its string, because after vendoring there is no separate add-on to open.
    Fourth: the phase-local dialog smoke action exists in `resources/lib/addon.py` and is scheduled
    for removal or for a debug-only gate.

    Also record two things that are not deviations but will surprise a reader: the module writes an
    undeclared runtime setting holding a service port for each of its four services, so an
    undeclared entry appears in the new profile's settings file on first service start; and the
    third-party traceback reporter is carried into the tree unchanged, a known liability whose
    gating setting already defaults to off, deleted in Phase 7 under KODI-08. Finally record the
    retry loop's worst-case wall time as the number of tries multiplied by the timeout plus the
    injected waits.

    **CREDITS.md.** State that this add-on originates from `plugin.onedrive` by Carlos Guzman
    (cguZZman), licensed GPL-3.0-or-later; that it bundles `script.module.clouddrive.common` by the
    same author, also GPL-3.0-or-later; that it bundles PyQRCode by Michael Nooner under
    BSD-3-Clause; and that PyQRCode in turn bundles pypng by Johann C. Rocholl and others under
    MIT. Name the Apache-2.0 file that travels with the cache subtree and note its unresolved
    provenance rather than asserting what it covers.

    **The coverage declaration.** `.planning/phases/01-vendor-lift/COVERAGE.md` already exists and
    records that this phase integrates no external API. Re-read it and confirm it is still
    accurate against what actually landed; if a plan introduced an external call this phase did not
    anticipate, amend it. Do not create a second copy at the repository root — the repository is
    the shippable add-on directory, and a planning artifact does not belong in the installed zip.

    **README.md.** Rewrite it. The current text advertises upstream features including the local
    directory-listing server this project deletes and a browser-based login this project replaces.
    Describe what this add-on is: a self-contained fork of `plugin.onedrive` shipping under a
    separate id so it installs alongside the original without touching it, carrying its own copy of
    the common module, depending on nothing outside `xbmc.python`, targeting Kodi 20, 21 and 22.
    State the current status honestly, including that sign-in is being rewritten in Phase 3 and
    does not work in this build. Include the attribution ID-04 requires and point at `CREDITS.md`
    and `VENDORED.md`. Do not describe features this project has decided to delete as if they were
    selling points.
  </action>
  <verify>
    <automated>python -m pytest tests/test_vendor_gates.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_vendor_gates.py -q` exits 0 with all 22 assertions passing
    - `grep -c 'df68e9a589a6faef2b3228f7520e77729bc05d9b' VENDORED.md` returns at least 1
    - `grep -c '^## Local modifications' VENDORED.md` returns 1
    - `grep -c '^## Service extension point' VENDORED.md` returns 1
    - `grep -c '^## Recorded behaviour deviations' VENDORED.md` returns 1
    - `grep -c 'allow_directory_listing' VENDORED.md` returns at least 1
    - `grep -c '_dialog_smoke' VENDORED.md` returns at least 1
    - `grep -c 'mkdirs' VENDORED.md` returns at least 1 and `grep -c 'uuid4' VENDORED.md` returns at least 1, proving both QR-image write guards reached the local-modification table rather than shipping as unrecorded edits to a vendored file
    - `grep -c 'source_scan' VENDORED.md` returns at least 1 and `grep -c 'test_no_hardcoded_module_id' VENDORED.md` returns at least 1, proving the reconciled form of the repository-wide sweep is written down where a later verification pass will find it
    - `grep -c 'Michael Nooner' CREDITS.md` returns at least 1 and `grep -c 'Johann C. Rocholl' CREDITS.md` returns at least 1
    - `test -s .planning/phases/01-vendor-lift/COVERAGE.md` exits 0 and `grep -c 'Phase 3' .planning/phases/01-vendor-lift/COVERAGE.md` returns at least 1
    - `test ! -e COVERAGE.md` exits 0 — the declaration is a planning artifact and must not be added to the shippable add-on directory
    - `python -c "t=open('README.md',encoding='utf-8').read(); assert 'cguZZman' in t or 'Carlos Guzman' in t; assert 'CREDITS.md' in t; print('ok')"` prints `ok`
    - `kodi-addon-checker . --branch=omega` runs to completion, reports no finding of severity ERROR, and its output is recorded verbatim in the SUMMARY. Every WARN finding is listed in the SUMMARY with a one-line disposition — fixed here, deferred to a named phase, or accepted with a reason. A criterion that only requires the tool to finish cannot fail, and this one has to be able to
  </acceptance_criteria>
  <done>The whole gate file is green, and the record distinguishes every deliberate decision from an oversight — including the one licence question this phase cannot answer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Install matrix across four Kodi versions and the acceptance pass on two platforms</name>
  <precondition>The environment recorded in 01-01-SUMMARY.md holds: `adb devices` still lists the Android test phone in state `device` and `adb pull` still retrieves its `kodi.log`, portable Kodi 19, 20 and 22 installs exist alongside Kodi 21.3, and `%APPDATA%\Kodi\addons` still contains none of plugin.onedrive, plugin.googledrive, plugin.dropbox or script.module.clouddrive.common</precondition>
  <read_first>
    - `.planning/phases/01-vendor-lift/01-01-SUMMARY.md` — the recorded environment inventory
    - `VENDORED.md` — the two recorded behaviour deviations, which the checklist names rather than treats as failures
    - `.planning/phases/01-vendor-lift/01-RESEARCH.md` section "Verification Tooling", subsections "Semi-automatable" and "Genuinely manual"
    - `.planning/phases/01-vendor-lift/01-VALIDATION.md` section "Manual-Only Verifications"
    - `.planning/research/ANDROID-PLAYBACK-STORAGE.md` section 7.6 — the deleted-profile and repeat-invocation checks folded into checklist step 4, and what each decides
  </read_first>
  <what-built>
    Everything automatable is already done and green: 22 assertions over the repository as text,
    XML and syntax tree, plus `python -m compileall` over the whole tree. What remains is what a
    text sweep structurally cannot reach — Kodi's own installer refusing a version, dialogs
    rendering through a skin engine, labels being readable, and Kodi's Android build resolving its
    own profile paths on a device rather than on a desktop. The dialog smoke action from Task 1
    exists precisely because the QR dialog
    is constructed deep inside a sign-in flow behind a dead broker and cannot be reached by
    clicking.
  </what-built>
  <how-to-verify>
    Package the working tree as a zip and run two separate result sets. Record them separately: a
    successful install is not a successful acceptance pass, and neither substitutes for the other.

    **Install matrix — four rows, one action each.**
    1. Kodi 19 Matrix (portable): attempt install from zip. Expected: the install is refused and
       the log names the unmet `xbmc.python` dependency. A successful install here is a failure of
       KODI-01.
    2. Kodi 20 Nexus (portable): install from zip. Expected: installs; the plugin appears in the
       add-on list; the service starts at login. Confirm from `kodi.log` that lines naming both
       `entrypoint.py` and `service.py` appear under the new add-on id, and grep the same log for
       `Traceback` and for an unknown-add-on-id error.
    3. Kodi 21.3 Omega (the existing Windows install, clean profile): same.
    4. Kodi 22 Piers (portable nightly): same. Record the nightly build string.

    **Acceptance checklist — one row per platform, Windows on Kodi 21.3 and the Android phone over
    `adb`.** Run the Windows pass first and the Android pass second. Do not treat a Windows pass as
    evidence for Android: Kodi's Android build — its storage regime, its profile paths, its file
    mode bits and its loopback binding — is not reproducible from a Windows desktop, and that is
    exactly what the Android leg exists to exercise. The phone stands in for the deployment box on
    every one of those OS-level questions, because they follow the API level rather than the form
    factor and the phone runs the same Android 11 or later storage regime as the box. It does not
    stand in for D-pad focus, ten-foot readability or low-end box performance; this phase asserts
    none of the three, and none may be recorded here as though it had been checked.
    1. Install on a clean profile with no sibling cloud-drive add-on and no external common module
       installed. Confirm the profile is clean before installing, not after.
    2. Open the plugin root. Expected: it renders an account list that is empty, with an
       add-an-account row, and no error dialog. Empty is the correct result on a fresh profile,
       not a failure.
    3. Open the settings screen. Read every category and every row and confirm each label reads
       correctly. This is the string-renumbering regression surface and it is the phase's largest
       silent risk: the two id spaces collided completely and 22 of the 26 ids meant different
       things on each side, so a mistake here shows as a plausible-looking wrong word rather than
       as an error. A slideshow context menu reading "Advanced", or an add-an-account row reading
       "Auto-Refreshed slideshow", is the shape of the failure.
    4. Delete the new add-on's `addon_data` directory from the platform under test, so the QR
       write meets a genuinely absent profile path — the case a brand-new add-on id newly creates
       and the one realistic cause of that write failing. On the phone that deletion is
       `adb shell rm -rf /sdcard/Android/data/org.xbmc.kodi/files/.kodi/userdata/addon_data/plugin.onedrive.kn`
       with Kodi closed. Then run the dialog smoke action on a plugin URL under the new add-on id.
       Expected: all three dialogs open and close, textures render, no traceback appears, and the
       QR image file appears under the new profile's add-on data directory — check the path, not
       just the picture. The smoke action constructs the QR dialog twice within the one session;
       quote **both** resolved image paths in the record and confirm they differ. On the phone,
       read both out of the pulled `kodi.log` rather than off the screen: a quoted log line is
       checkable where a transcribed one is not, and pulling the log is what makes that possible
       here. Two identical paths, or a second dialog showing the first dialog's image, means the
       per-invocation filename guard did not take effect. This is
       `.planning/research/ANDROID-PLAYBACK-STORAGE.md` section 7.6 run as part of the acceptance
       pass rather than as a separate errand.
    5. Confirm the service starts at login and that a full pass leaves no `Traceback` and no
       unknown-add-on-id error in `kodi.log`.
    6. On the phone only: walk the whole add-on end to end and confirm every screen is reachable
       and renders under Kodi's Android build. Record what was walked. Do not record anything
       about D-pad focus order or ten-foot readability: the phone cannot answer either question
       and this phase does not ask it. Both belong to the deployment box and are checked before
       release under CI-07.
    7. Confirm the two recorded deviations rather than reporting them as regressions: sign-in
       cannot complete because the broker is dead, so account listing, browsing and playback are
       unreachable; and the directory listing is off by default. Both are recorded in
       `VENDORED.md`.

    Capture `kodi.log` from both platforms after a full pass rather than incrementally. On Android
    that is one command — `adb pull /sdcard/Android/data/org.xbmc.kodi/files/.kodi/temp/kodi.log`
    — so the Android log arrives as a file to grep rather than as something read off a screen.
    Attach both to the phase record. No traceback in a full pass is the single most valuable
    artifact this phase produces, because every later phase will ask whether something was already
    broken after vendoring.
  </how-to-verify>
  <acceptance_criteria>
    - The Kodi 19 install attempt is refused and the log line naming the unmet `xbmc.python` dependency is quoted verbatim in the record
    - Kodi 20, 21.3 and 22 each install, and for each the `kodi.log` lines naming both entry points under the new add-on id are quoted in the record
    - `grep -c Traceback` over each of the four logs returns 0, and the counts are recorded per platform
    - The acceptance checklist has a filled row for Windows on Kodi 21.3 and a filled row for the Android phone over `adb`; an unfilled row is a phase-gate failure
    - The QR image path under the new profile's add-on data directory is quoted verbatim, showing the new add-on id in the path
    - Both QR image paths from the twice-constructed dialog are quoted for each platform and they are different from each other; two identical paths is a phase-gate failure, not a note. On Android both are quoted from the pulled `kodi.log`, so the comparison is made against a retrieved file rather than against a transcription
    - The `addon_data` directory was deleted before the smoke run on each platform, and the run produced no traceback — proving the profile directory is created before the image write rather than assumed to exist. On Android the `adb shell rm -rf` command that performed the deletion is recorded verbatim
    - Every settings row label is confirmed read on both platforms, and any label that reads wrongly is reported with its id
    - Both `kodi.log` files are archived with the phase record, the Android one retrieved with `adb pull` and its local path recorded
    - The two recorded deviations are named in the report rather than filed as defects
  </acceptance_criteria>
  <resume-signal>Report the four install outcomes, the two acceptance rows, both QR image paths per platform from the twice-constructed dialog, the per-log traceback counts, and any label that read wrongly — or describe what blocked the pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository record -> future reader | `VENDORED.md` is the only thing that distinguishes a deliberate deviation from an oversight for every later phase |
| debug affordance -> shipped build | The dialog smoke action constructs UI with dummy arguments and is reachable on a plugin URL |
| built zip -> Kodi installer | The manifest is the only mechanism that can refuse installation on an unsupported Kodi |
| Android application data directory -> other applications | The profile, and therefore the QR image and the account store, resolves under external storage. From Android 11 an app's own `Android/data` bypasses FUSE and is closed to other applications, which is the regime on both the test phone and the deployment box; on an Android 9 or 10 box it is not, and the design still assumes that worst case |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-21 | Elevation of Privilege | The phase-local dialog smoke action reachable in a shipped build | medium | mitigate | The action lives in this repository's own file rather than the vendored tree so it is trivially removable, is marked phase-local in a code comment, and is recorded in `VENDORED.md` with removal or a debug-only gate scheduled. `test_vendored_md_sections` asserts the deviations section exists and an acceptance criterion greps `VENDORED.md` for the action name. It constructs dialogs with dummy arguments and performs no network call and no credential access |
| T-1-03 | Information Disclosure | The directory listing, defaulted off in plan 01-03 | high | mitigate | The acceptance pass confirms the default is off in a fresh profile rather than trusting the manifest, and `VENDORED.md` records the deviation with a forward reference to KODI-07 so it is not mistaken for a regression and not forgotten before deletion |
| T-1-22 | Repudiation | An undocumented behaviour change inside the vendor commit | high | mitigate | The local-modification table is keyed by file and assembled from the three preceding plan records rather than re-derived, and the deviations section enumerates every departure. A prohibition in this plan forbids an unrecorded behaviour change outright |
| T-1-23 | Spoofing | Installing on a contaminated profile and reading the result as a pass | high | mitigate | The checkpoint's precondition re-asserts the clean-profile facts recorded in 01-01 before the pass begins, and step 1 of the checklist confirms the profile is clean before installing rather than afterwards. Eight add-on-id lookups keep resolving on any machine with a sibling cloud-drive add-on installed, so a contaminated profile turns the entire acceptance pass into evidence for nothing |
| T-1-24 | Information Disclosure | Archived Kodi logs attached to the phase record | low | accept | The logs come from a maintainer test profile with no account signed in, because the broker is dead and sign-in cannot complete. Rationale for acceptance: no token, tenant name or user principal name can appear in a log from a profile that never authenticated; this rationale stops holding at Phase 3 and the log-handling practice is revisited there |
| T-1-SC | Tampering | Package-manager installs | low | accept | This plan installs no package-manager dependency. The phase's only package-manager install is the development tool `kodi-addon-checker` in plan 01-01, registry verdict OK with no ASSUMED or SUS finding, so no blocking legitimacy checkpoint is required |
</threat_model>

<flagged_assumptions>
Probe edges that remain unresolved and are surfaced rather than silently dropped or backstopped.

| Requirement | Probe category | Why it stays unresolved |
|---|---|---|
| VND-07 | unclassified | The probe taxonomy asks about data shape. "An explicit decision is recorded about what survives the fold" is a claim about a document's content, with no empty, encoding, ordering, adjacency, concurrency or idempotency dimension. It is asserted directly by `test_vendored_md_sections`, which requires the service section to name both the service class and its redirector variant. The residual assumption is that the module's manifest declared exactly one service entry point running exactly that construction — read from the upstream source at the pinned commit and recorded as verified, but not re-derivable from this repository once the upstream file is deliberately not copied |
| ID-04 | unclassified | "The credits name the origin and the bundled module" is a document-content claim with no data-shape dimension. It is asserted directly by `test_credits_content`, which greps for each required name and licence. The residual assumption is that the four attributions named are the complete set — that no further third-party code is embedded inside the vendored tree beyond the QR encoder and the PNG writer bundled inside it. `test_vendor_tree_self_contained` and `test_gpl_headers_intact` are what convert that assumption into a check |
| KODI-02 | unclassified | "The add-on installs and runs on Kodi 20, 21 and 22" is a runtime claim about three external platforms, with no data-shape dimension the probe taxonomy can reach. It is proven only by the install matrix in Task 3. The residual assumption is that a portable install exercises the same dependency resolution and the same entry-point loading as a normal install; portable mode is long-standing Kodi behaviour but was not exercised during research, and if it turns out not to hold, the matrix needs virtual machines or a second machine |

Edge tally for this plan: 10 probe edges applicable (VND-07 unclassified; VND-09 concurrency;
VND-10 adjacency, empty and ordering; ID-04 unclassified; KODI-02 unclassified; CI-06 adjacency,
empty and ordering). 7 authored into `must_haves.truths` — five covered and two structured
backstop markers, one for VND-10 ordering and one for CI-06 ordering — and 3 surfaced above as
flagged assumptions. 7 + 3 = 10. No edge was dropped.

**Phase-wide edge tally.** The deterministic probe proposed 38 applicable edges across the 20
phase requirements. Across all seven plans: 30 are authored into `must_haves` — 23 as plain
covered truths and 7 as structured backstop markers — and 8 are surfaced as flagged assumptions,
one per `unclassified` row (SETUP-06, KODI-01, VND-01, VND-03, VND-11, VND-07, ID-04, KODI-02).
30 + 8 = 38. No edge was auto-dismissed and none was dropped.
</flagged_assumptions>

<artifacts_this_phase_produces>
Every symbol this phase creates, listed in full so downstream source-grounding passes do not
flag newly-created names as drift. This plan creates `VENDORED.md`, `CREDITS.md` and the
`_dialog_smoke` action.

**New files**
- `pytest.ini`
- `tests/test_vendor_gates.py`
- `VENDORED.md`
- `CREDITS.md`
- `.planning/phases/01-vendor-lift/COVERAGE.md` — planning artifact, written at plan time, not shipped
- `resources/lib/vendor/__init__.py`
- `resources/lib/vendor/clouddrive_common/` — `__init__.py`, `account.py`, `db.py`, `export.py`, `exception.py`, `html.py`, `utils.py`, `cache/__init__.py`, `cache/cache.py`, `cache/LICENSE`, `remote/__init__.py`, `remote/errorreport.py`, `remote/oauth2.py`, `remote/provider.py`, `remote/request.py`, `remote/signin.py`, `service/__init__.py`, `service/base.py`, `service/download.py`, `service/export.py`, `service/player.py`, `service/source.py`, `service/utils.py`, `ui/__init__.py`, `ui/addon.py`, `ui/dialog.py`, `ui/logger.py`, `ui/utils.py`
- `resources/lib/vendor/pyqrcode/` — `__init__.py`, `builder.py`, `tables.py`, `png.py`, `LICENSE.md`
- `resources/skins/default/1080i/` — `pin-dialog.xml`, `export-main-dialog.xml`, `export-schedule-dialog.xml`
- `resources/skins/default/media/` — `black.png`, `dialog-bg.png`, `white.png`, `dialogbutton-fo.png`, `dialogbutton-nofo.png`, `radio-button-on.png`, `radio-button-off.png`

**New runtime symbols**
- Plugin action `_dialog_smoke` and the method `OneDriveAddon._dialog_smoke` in `resources/lib/addon.py`
- Class attribute `HTTP_TIMEOUT_SECONDS` on the request class in `resources/lib/vendor/clouddrive_common/remote/request.py`

**New test functions** (all in `tests/test_vendor_gates.py`)
`test_license_unmodified`, `test_gpl_headers_intact`, `test_addon_xml_identity`,
`test_addon_xml_imports`, `test_addon_id_everywhere`, `test_string_ids_partitioned`,
`test_directory_listing_default_off`, `test_no_legacy_package_prefix`,
`test_no_hardcoded_module_id`, `test_no_unanchored_rename_damage`, `test_resources_merged`,
`test_no_syspath_mutation`, `test_vendor_tree_self_contained`, `test_tree_compiles`,
`test_no_eval`, `test_store_json_roundtrip`, `test_all_http_calls_have_timeout`,
`test_skin_assets_present`, `test_licences_present`, `test_vendored_sha_recorded`,
`test_vendored_md_sections`, `test_credits_content`

**Renumbered string ids** (this add-on's own strings, moved from the 32000 block to the 30000 block)
- `en_gb`: 30000, 30001, 30002, 30003, 30004, 30005, 30006, 30007, 30008, 30009, 30010, 30011, 30017, 30018, 30019, 30020, 30030, 30031, 30032, 30033, 30034, 30035, 30067, 30068, 30069 — 25 ids
- `he_il`: 30000 through 30011 — 12 ids
- `32012` is deleted rather than renumbered, together with the settings row it labelled
- The vendored module's ids stay at 32000 through 32088 in both files, untouched
</artifacts_this_phase_produces>

<verification>
- `python -m pytest tests/test_vendor_gates.py -q` exits 0 with all 22 assertions green
- `python -m compileall -q resources/ entrypoint.py service.py` exits 0
- `kodi-addon-checker . --branch=omega` reports no ERROR finding and its output is recorded verbatim, with every WARN finding dispositioned
- `VENDORED.md` carries all six required headings, the pinned commit, the exclusion list with reasons, the local-modification table keyed by file including the two QR-image write guards, the reconciled form of the repository-wide sweep, the service-extension-point decision and all four recorded deviations
- `CREDITS.md` names the origin, the bundled module, the QR encoder, the PNG writer bundled inside it, and each licence
- `.planning/phases/01-vendor-lift/COVERAGE.md` still states the reasoned no-external-API declaration accurately, and no copy of it exists at the repository root
- The install matrix has four filled rows and the acceptance checklist has two filled platform rows
- The smoke run on each platform started from a deleted `addon_data` directory, produced no traceback, and logged two different QR image paths
- Both `kodi.log` files are archived and neither contains a traceback, the Android one pulled off the device with `adb pull`
</verification>

<success_criteria>
The phase closes with a record a stranger could act on and a result a later phase can rely on:
the vendored tree's provenance is pinned to a commit, every local modification is listed by file,
every deliberate deviation from identical behaviour is written down with its reasoning and a
forward reference, and the add-on has been installed on four Kodi versions and driven end to end
on Windows and on the Android test phone over `adb` on a clean profile, with both logs archived —
the Android one pulled straight off the device — and no traceback in either. The deployment box
is not part of this gate; it is exercised by use and checked before release under CI-07.
</success_criteria>

<output>
Create `.planning/phases/01-vendor-lift/01-07-SUMMARY.md` when done. Record the four install
outcomes with the quoted log lines, both acceptance rows, both QR image paths per platform showing
the new add-on id and differing from each other, the per-log traceback counts, the
`kodi-addon-checker` output with a disposition for every WARN finding, Task 1's commit SHA, and
the paths to both archived logs.
</output>
